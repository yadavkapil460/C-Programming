#include <stdio.h>
int main()
{
    int arr[10] = {1,2,3,33,45,65,98,78,56,89};
    int sum = 0;
    for(int i=0 ; i<10 ; i++){
     sum = sum + arr[i];
    }
    printf("your sum of element is : %d",sum);
    return 0;
}
